# ImageKit Environment Variables
# These need to be set in Vercel Dashboard > Settings > Environment Variables
# IMPORTANT: Set for Production environment AND Preview environment
# Make sure to check "Automatically expose" or similar options if available

# Public key (starts with "public_")
NEXT_PUBLIC_IMAGEKIT_PUBLIC_KEY=

# Private key (starts with "private_")
NEXT_PUBLIC_IMAGEKIT_PRIVATE_KEY=

# URL endpoint format: https://ik.imagekit.io/your-name
NEXT_PUBLIC_IMAGEKIT_URL_ENDPOINT=

# Folder names to fetch images from (comma-separated, no spaces)
NEXT_PUBLIC_IMAGEKIT_FOLDERS=January,November
